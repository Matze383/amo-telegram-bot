name: yt_rss
version: "0.1.0"
description: Topic-scoped YouTube channel RSS subscriptions (skeleton)
commands:
  - addyt
  - delyt
schedule:
  interval_seconds: 300
required_roles:
  - admin
  - owner
required_permissions:
  - send_message
  - rss.fetch
settings_schema: {}
